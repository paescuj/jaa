import { Link } from '@chakra-ui/react';
import NextLink from 'next/link';

import { ChatToggle } from '../components/Chat';
import JobPopup from '../components/JobPopup';

<div>
Es freut mich, Ihnen meine Bewerbungsunterlagen für die Stelle als <JobPopup link={props.job.link} previewUrl={props.job.preview[0]}><strong>{props.job.position}</strong></JobPopup> auf diesem Wege präsentieren zu dürfen.

Sie haben hier die Möglichkeit, die Unterlagen interaktiv zu begutachten, <span style={{ display: props.chat ? 'inline' : 'none' }}>mit mir in <ChatToggle variant="link" colorScheme="black">Kontakt</ChatToggle> zu treten</span> oder auch mehr <NextLink href="/about" passHref><Link fontWeight="bold">über diese Applikation</Link></NextLink> zu erfahren.

</div>
